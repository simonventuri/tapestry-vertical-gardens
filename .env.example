# Redis Configuration
# For local development (default)
REDIS_URL=redis://localhost:6379

# For production hosting (replace with your provider's Redis URL)
# REDIS_URL=redis://username:password@hostname:port

# Example for various providers:
# Heroku Redis: redis://h:p123456789abcdef@ec2-123-45-67-89.compute-1.amazonaws.com:12345
# Railway: redis://default:password@redis.railway.internal:6379
# Render: redis://red-abc123def456:6379

# Email Configuration
# SMTP settings for sending contact form emails
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@tapestryverticalgardens.com

# For Gmail, you'll need to:
# 1. Enable 2-factor authentication
# 2. Generate an app-specific password
# 3. Use the app password in EMAIL_PASS

# Alternative email providers:
# SendGrid: EMAIL_HOST=smtp.sendgrid.net, EMAIL_PORT=587
# Mailgun: EMAIL_HOST=smtp.mailgun.org, EMAIL_PORT=587
# Outlook: EMAIL_HOST=smtp-mail.outlook.com, EMAIL_PORT=587
